def plusOne(number):
    return number + 1

newNumber = plusOne(5)
print(newNumber)

# functions are a like a mini-program inside your program
# the main point of functions is to get rid of duplicate code
# the def statement defines a function
# the input to dunctions are arguments. the output is the return value
# the parameters are the variables in between the function's parenthesis in the def statement
# the return value is specified using the return statement
# every function has a return value | if your function doesn't have a return statement, the default return value is None
# keyword arguments to functions are usually for optional arguments | the print() function has keyword argumentsend and sep
